# Python testing lessons

- Task environments usually have no network: do not try to `pip install` missing packages; check what is installed with `python3 -m pip list` first.
- `pytest -x` stops at the first failure and keeps the output short.
